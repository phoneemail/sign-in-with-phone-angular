export const environment = {
    production: false,
    countryCode: "YOUR_COUNTRY_CODE", //Replace the country code
    phoneNo: 'YOUR_PHONE_NUMBER',       //Replace the phone number
    redirectURL: 'YOUR_REDIRECT_URL',   //Replace the redirect URL
    getEmailCountURl: 'https://eapi.phone.email/email-count',
    apiKey: 'YOUR_API_KEY'              //Replace the API key
};
